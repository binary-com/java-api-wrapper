language: java
notification:
  email: false
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
before_script:
    - git clone ssh://git@github.com:regentmarkets/java-api-wrapper-config.git ~/binary-api-wrapper/src/test/resources
after_success:
    - ./gradlew jacocoTestReport coveralls
